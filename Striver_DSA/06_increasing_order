# using bubble sorted increasing
n = int(input())
arr = list(map(int,input().split()))

for i in range(n-1):
    is_sorted  = True
    for j in range(n-i-1):
        if arr[j] > arr[j + 1]:
            is_sorted = False
            arr[j], arr[j+1] = arr[j+1], arr[j]
print(*arr)
